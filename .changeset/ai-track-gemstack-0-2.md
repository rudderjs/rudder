---
"@rudderjs/ai": patch
---

Track `@gemstack/ai-sdk@0.2.0`: bump the dependency range from `^0.1.0` to `^0.2.0` so the deprecated `@rudderjs/ai` shim forwards the decoupled release (the SDK's only required runtime dependency is now `zod`). No API change.
