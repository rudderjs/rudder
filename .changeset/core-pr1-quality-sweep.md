---
"@rudderjs/core": patch
---

Fix XSS in HTML error pages, double provider registration before bootstrap, fictional `events` export in docs, and FormRequest pipeline order docs. Add `EventFake` documentation. Warn on async deferred provider boot.
