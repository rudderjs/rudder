model Workspace {
  id          String   @id @default(cuid())
  name        String
  description String?
  nodes       String   @default("{}")    // JSON: flat node map (departments, agents, KB, connections)

  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  @@map("workspaces")
}
