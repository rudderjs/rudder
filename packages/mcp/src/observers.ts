// Re-export the observer registry from the core so the `@rudderjs/mcp/observers`
// subpath (consumed by Telescope and other collectors) keeps working unchanged.
export * from '@gemstack/mcp/observers'
