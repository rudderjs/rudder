name: CI

on:
  push:
    branches: [main]
    paths-ignore:
      - '**/*.md'
      - '**/*.mdx'
      - 'docs/**'
      - 'LICENSE'
      - '.gitignore'
      - '.github/ISSUE_TEMPLATE/**'
      - '.github/CODEOWNERS'
  pull_request:
    branches: [main]
    paths-ignore:
      - '**/*.md'
      - '**/*.mdx'
      - 'docs/**'
      - 'LICENSE'
      - '.gitignore'
      - '.github/ISSUE_TEMPLATE/**'
      - '.github/CODEOWNERS'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Lint
        run: pnpm lint

  build-test:
    name: Build & Test (${{ matrix.os }}, Node ${{ matrix.node-version }})
    runs-on: ${{ matrix.os }}
    timeout-minutes: 20
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
        node-version: ['20', '22']

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Cache turbo artifacts
        uses: actions/cache@v4
        with:
          path: node_modules/.cache/turbo
          key: turbo-${{ runner.os }}-node${{ matrix.node-version }}-${{ github.sha }}
          restore-keys: |
            turbo-${{ runner.os }}-node${{ matrix.node-version }}-

      - name: Build
        run: pnpm turbo run build --filter='./packages/*' --filter='./create-rudder-app'

      - name: Test
        run: pnpm turbo run test --filter='./packages/*' --filter='./create-rudder-app'

  docs-build:
    name: Docs Build
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build docs site
        # Docs build catches VitePress/Vue-parser breakage in `.md` files
        # (plan docs accidentally landing `Promise<T>` or `<5%` patterns, etc.).
        # The release workflow runs this as part of `pnpm build` and used to
        # be the first place such breakage surfaced — now it's caught at PR time.
        # Pulled out of build-test so it doesn't run 4× per matrix cell.
        run: pnpm --filter @rudderjs/docs build

  scaffolder-e2e:
    # Mirrors the user-facing recipes in `create-rudder-app/src/cli-flags.ts`:
    # every cell here is a profile a real user can pick at `pnpm create
    # rudder-app`. The matrix is react-heavy (all 5 recipes × react) +
    # narrow on vue/solid (only web-app, since minimal/api-service are
    # no-frontend and the framework override is ignored for `frameworks: []`).
    # The flow-check (register → home → sign-out) runs only on
    # react × web-app (smoke gates internally).
    name: Scaffolder E2E (${{ matrix.framework }} / ${{ matrix.profile }})
    runs-on: ubuntu-latest
    timeout-minutes: 25
    strategy:
      fail-fast: false
      matrix:
        include:
          # React — every recipe.
          - { framework: react, profile: minimal     }
          - { framework: react, profile: web-app     }
          - { framework: react, profile: saas        }
          - { framework: react, profile: api-service }
          - { framework: react, profile: realtime    }
          # Vue / Solid — web-app only. minimal + api-service ignore the
          # framework axis (no-frontend → vanilla welcome shell shared across).
          - { framework: vue,   profile: web-app     }
          - { framework: solid, profile: web-app     }

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Read Playwright version
        id: playwright
        # Keyed-by-version cache below: chromium binary churns when the
        # devDep version bumps. The shell parse keeps the workflow source
        # of truth in package.json instead of a duplicate string here.
        run: |
          version=$(node -p "require('./create-rudder-app/package.json').devDependencies.playwright.replace(/^[^0-9]*/, '')")
          echo "version=$version" >> "$GITHUB_OUTPUT"

      - name: Cache Playwright browsers
        id: playwright-cache
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-${{ runner.os }}-${{ steps.playwright.outputs.version }}

      - name: Install Playwright chromium
        # Skip the ~150MB browser download on cache hit. --with-deps still runs
        # so system libs land even when binaries were cached.
        if: steps.playwright-cache.outputs.cache-hit != 'true'
        run: pnpm --filter create-rudder-app exec playwright install --with-deps chromium

      - name: Install Playwright system deps (cache hit)
        if: steps.playwright-cache.outputs.cache-hit == 'true'
        run: pnpm --filter create-rudder-app exec playwright install-deps chromium

      - name: Build packages + scaffolder
        run: pnpm turbo run build --filter='./packages/*' --filter='./create-rudder-app'

      - name: Scaffold + boot + render-check
        run: pnpm --filter create-rudder-app smoke --framework=${{ matrix.framework }} --profile=${{ matrix.profile }}
