diff --git a/dist/node/vite/plugins/build/pluginProdBuildEntry.js b/dist/node/vite/plugins/build/pluginProdBuildEntry.js
index c84ab47949ca4307d42e64dc4f4464f1add09947..ae1f78f37d8d5d3e857ab089a8371364a568c3f6 100644
--- a/dist/node/vite/plugins/build/pluginProdBuildEntry.js
+++ b/dist/node/vite/plugins/build/pluginProdBuildEntry.js
@@ -65,6 +65,9 @@ function getServerProductionEntryCode(config) {
     ].join('\n');
     return importerCode;
 }
+// REPRO BRANCH ONLY (vikejs/vike rolldown __VITE_ASSETS_MANIFEST__ bug): this is the
+// STOCK upstream implementation, restored so `pnpm build` reproduces the prerender
+// `ReferenceError` on Linux. The real fix (walk outDir on disk) lives on `main`.
 // Set the value of the ASSETS_MANIFEST constant inside dist/server/entry.js (or dist/server/index.js)
 async function set_macro_ASSETS_MANIFEST(assetsJsonFilePath, bundle, outDir) {
     assert(outDir);
diff --git a/dist/node/vite/plugins/pluginDev/optimizeDeps.js b/dist/node/vite/plugins/pluginDev/optimizeDeps.js
index 21465d0480aeb7e754983942543794b0ef3d4315..5fe34d4c473c5ed8f6ec0b119bdb35fb448f78c9 100644
--- a/dist/node/vite/plugins/pluginDev/optimizeDeps.js
+++ b/dist/node/vite/plugins/pluginDev/optimizeDeps.js
@@ -133,7 +133,11 @@ async function getPageDeps(config, pageConfigs) {
     const addEntry = (e, isForClientSide, definedAt) => {
         assert(e);
         // optimizeDeps.entries expects filesystem absolute paths
-        assert(isVirtualFileId(e) || isFilePathAbsoluteFilesystem(e));
+        // RudderJS patch: an extension's `client` config can resolve to a bare
+        // package specifier (e.g. vike-react-rsc/...), which is neither virtual
+        // nor absolute. Upstream asserts and crashes RSC dev; skip it instead —
+        // Vite resolves such imports on demand (see vike's own getPageDeps note).
+        if (!isVirtualFileId(e) && !isFilePathAbsoluteFilesystem(e)) return;
         if (isExcluded(e, isForClientSide, definedAt))
             return;
         if (isForClientSide) {
